Build this application by doing the following:

// initialize npm:
npm init -y

// add express, mongoose, brycptjs, jsonwebtoken, dotenv, nodemon
npm install express mongoose bcryptjs json webtoken dotenv nodemon